package structures.node;

public enum NodeType {
    ASSIGNMENT,
    BLOCK,
    EVENT,
    EXPRESSION,
    FUNCTION,
    IF_STATEMENT,
    INT_LITERAL,
    REPEAT_IF_STATEMENT,
    REPEAT_STATEMENT,
    STRING_LITERAL,
    VARIABLE
}
